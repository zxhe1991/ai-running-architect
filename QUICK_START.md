# 快速开始指南

## ✅ 模拟响应已启用

`.env` 文件中已添加 `USE_MOCK_RESPONSE=true`，应用现在将使用模拟响应。

## 🚀 启动应用

1. **重启 Streamlit 应用**（如果正在运行）：
   ```bash
   # 停止当前应用（Ctrl+C）
   # 然后重新启动
   streamlit run app.py
   ```

2. **测试功能**：
   - 上传 `Running_Today.csv` 文件
   - 输入主观感受（可选）
   - 点击"分析并获取建议"
   - 现在应该能看到完整的教练建议！

## 📋 功能验证清单

- [ ] 应用启动成功
- [ ] 上传 CSV 文件成功
- [ ] 点击"分析并获取建议"后显示：
  - [ ] **即时评估与下次训练**（有内容）
  - [ ] **详细训练计划**（有内容）
  - [ ] **训练策略与原理**（有内容）
- [ ] 所有内容都是中文（如果选择了中文）

## 🔄 切换到真实 API

当 API 修复后，在 `.env` 文件中修改：
```
USE_MOCK_RESPONSE=false
```

然后重启应用即可。

## 📝 注意事项

- 模拟响应基于您的实际数据生成个性化建议
- 模拟响应包含完整的训练计划（8周）
- 模拟响应会根据您的语言设置自动切换中英文

## 🐛 如果遇到问题

1. 检查 `.env` 文件中 `USE_MOCK_RESPONSE=true`
2. 确保已重启 Streamlit 应用
3. 查看终端是否有错误信息
