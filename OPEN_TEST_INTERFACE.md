# 🎤 测试界面访问指南

## ✅ 测试界面已启动！

### 🌐 访问地址
**http://localhost:8502**

### 📋 如果浏览器没有自动打开：

1. **手动打开浏览器**
   - 打开任何浏览器（Chrome, Edge, Firefox 等）
   - 在地址栏输入：`http://localhost:8502`
   - 按 Enter

2. **或者点击这个链接**（如果支持）
   - [http://localhost:8502](http://localhost:8502)

### 🔍 检查服务器状态

如果无法访问，请检查：

1. **服务器是否正在运行**
   ```bash
   netstat -ano | findstr :8502
   ```
   如果看到输出，说明服务器正在运行

2. **重新启动服务器**
   ```bash
   streamlit run test_voice_interface.py --server.port 8502
   ```

### 📱 测试界面功能

界面包含以下测试部分：

1. **语音转文字测试** - 测试基本的语音转文字功能
2. **历史跑步信息输入测试** - 测试历史信息的文字/语音输入
3. **主观感受输入测试** - 测试主观感受的文字/语音输入
4. **测试总结** - 查看所有输入内容

### 🎯 快速测试步骤

1. 打开 http://localhost:8502
2. 准备一个音频文件（MP3/WAV，10-30秒即可）
3. 在"语音转文字测试"部分上传音频
4. 点击"开始转换"
5. 查看转换结果

### ⚠️ 如果遇到问题

- **端口被占用**：尝试使用其他端口（8503, 8504等）
- **API Key 错误**：检查 .env 文件中的 SUPER_MIND_API_KEY
- **无法连接**：确保防火墙允许访问 localhost:8502

### 💡 提示

- 测试界面与主应用（端口8501）独立运行，不会冲突
- 可以同时打开两个界面进行对比测试
- 建议先测试简短的音频文件（10-30秒）
