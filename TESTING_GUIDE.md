# 测试指南 - AI Running Architect

## ✅ 数据持久化测试

### 1. 测试用户设置保存
1. 打开应用：http://localhost:8501
2. 在侧边栏输入：
   - 年龄：30
   - 性别：男性
   - 目标配速：5:00
   - 目标日期：In 3 months
   - 每周训练时间：5 小时
   - 配速单位：km
   - 语言：中文
3. **关闭浏览器标签页**（不要关闭终端）
4. **重新打开** http://localhost:8501
5. **验证**：所有设置应该自动加载

### 2. 测试历史数据加载
1. 检查侧边栏底部是否显示：
   - ✅ "历史数据已就绪"
   - "已索引 X 次跑步记录"
2. 如果显示 ⚠️ "历史数据尚未构建"：
   - 上传 `Garmin_Runing.csv` 文件
   - 点击 "构建历史数据索引"
   - 等待完成

### 3. 测试完整流程
1. **设置用户信息**（侧边栏）
2. **上传历史数据**（如果还没有）
3. **上传今日 TCX 文件**
4. **输入主观感受**
5. **点击"分析并获取建议"**
6. **验证结果**：
   - 关键指标显示
   - 历史对比显示
   - AI 教练建议显示

## 🔍 验证数据文件

### 检查文件是否存在：
```bash
# Windows PowerShell
Test-Path user_config.json
Test-Path garmin.index
Test-Path garmin_data.pkl
```

### 查看保存的配置：
```bash
# Windows PowerShell
Get-Content user_config.json
```

## 🐛 常见问题

### 问题：设置没有保存
- **解决**：确保应用有写入权限
- **检查**：查看终端是否有错误信息

### 问题：历史数据丢失
- **原因**：`garmin.index` 或 `garmin_data.pkl` 文件被删除
- **解决**：重新上传 CSV 并构建索引

### 问题：应用重启后数据丢失
- **检查**：`user_config.json` 文件是否存在
- **验证**：文件内容是否正确

## 📝 测试清单

- [ ] 用户设置可以保存
- [ ] 应用重启后设置自动加载
- [ ] 历史数据索引可以检测到
- [ ] 历史数据搜索功能正常
- [ ] TCX 文件分析功能正常
- [ ] AI 教练建议可以生成
- [ ] 所有界面文本正确显示（中英文切换）
